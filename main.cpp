#include <iostream>
#include "utils/Server.hpp"
int main(int argc, char** argv) {
        if (argc != 3) {
            std::cout << "should be like: ./server port doc_root" << std::endl;
            return 1;
        }

        //create server

    return 0;
}